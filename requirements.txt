# pyproject.toml is the source of truth for dependencies — install with extras:
#
#   pip install -e .                                        # Phase 0 core only
#   pip install -e .[voice,stt,skills,tray,miku]            # everything the tray runs
#   pip install -e .[mascot3d]                              # + fetch script for the 3D mascot
#   pip install -e .[dev]                                   # pytest
#
# The 3D mascot itself is a Node app:  cd mascot3d && npm install
#
# Kept only so `pip install -r requirements.txt` still bootstraps Phase 0:
openai>=1.40
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
